<!DOCTYPE html>
<html lang="pt-BR">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Relatório - <%= dados?.nome_empresa || 'Empresa' %></title>
        <script src="https://cdn.tailwindcss.com"></script>

        <style>
            /* === ESTILOS ORIGINAIS === */
            @page {
                size: A4;
                margin: 0;
            }
            body {
                font-family: Arial, sans-serif;
            }
            .page-break {
                page-break-before: always;
                page-break-inside: auto;
                position: relative;
                overflow: visible;
                height: auto;
                min-height: 100vh;
                width: 100%;
            }

            /* Primeira página não força quebra */
            .page-break:first-child {
                page-break-before: auto;
            }

            /* Para conteúdo longo como listas */
            .section-break-inside {
                page-break-inside: auto;
                break-inside: auto;
            }

            /* Itens de lista que podem quebrar entre eles, mas não no meio */
            .breakable-list li {
                page-break-inside: avoid;
                break-inside: avoid;
            }

            /* === ESTILOS ESPECÍFICOS PARA PDF - ADICIONE ESTA SEÇÃO === */

            /* Garante que cada página seja independente */
            .page-break {
                page-break-before: always;
                page-break-inside: avoid;
                position: relative;
                overflow: hidden;
                height: 100vh;
                width: 100%;
            }

            /* Container para elementos decorativos */
            .decorative-container {
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                overflow: hidden;
                z-index: 0;
                pointer-events: none;
            }

            /* Força contenção de elementos absolutos */
            .page-break > * {
                position: relative;
                z-index: 1;
            }

            /* Estilos específicos para elementos de fundo */
            .bg-decoration {
                position: absolute;
                z-index: 0;
            }

            /* Garante que imagens sejam renderizadas corretamente */
            img {
                max-width: 100%;
                height: auto;
                display: block;
            }

            /* Previne quebra de elementos importantes */
            .no-break {
                page-break-inside: avoid;
                break-inside: avoid;
            }

            /* Estilos para Puppeteer específicos */
            @media print {
                .page-break {
                    page-break-before: always;
                    height: auto;
                    overflow: visible;
                }

                /* Força renderização de backgrounds em PDF */
                * {
                    -webkit-print-color-adjust: exact;
                    print-color-adjust: exact;
                }

                /* Garante que gradientes sejam renderizados */
                .bg-gradient-to-r {
                    -webkit-print-color-adjust: exact;
                    print-color-adjust: exact;
                }
            }

            /* Container específico para a página objetivo */
            .objetivo-page {
                position: relative;
                min-height: 100vh;
                overflow: hidden;
            }
            /* Círculo decorativo com contenção melhorada */
            .circle-decoration {
                position: absolute;
                top: -50px;
                left: -50px;
                width: 200px;
                height: 200px;
                border: 6px solid #3b82f6;
                border-radius: 50%;
                z-index: 0;
                overflow: hidden;
            }

            .circle-decoration::before {
                content: "";
                position: absolute;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
                width: 100px;
                height: 100px;
                border: 4px solid #3b82f6;
                border-radius: 50%;
                background-color: rgba(191, 219, 254, 0.3);
            }

            /* Bolinhas decorativas */
            .dots-decoration {
                position: absolute;
                top: 20px;
                left: 30px;
                z-index: 1;
                display: grid;
                grid-template-columns: repeat(2, 1fr);
                gap: 12px;
            }

            .dot {
                width: 12px;
                height: 12px;
                background-color: #3b82f6;
                border-radius: 50%;
            }

            /* Corrigir posicionamento de elementos absolutos no PDF */
            .min-h-screen {
                position: relative;
                overflow: hidden;
            }

            /* Garante que elementos absolutos não vazem para outras páginas */
            .relative {
                position: relative;
                z-index: 1;
            }

            /* Força renderização de cores de fundo */
            .bg-blue-500,
            .bg-blue-600,
            .bg-blue-400,
            .bg-blue-300 {
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }

            /* Garante que bordas sejam renderizadas */
            .border-blue-300,
            .border-blue-500,
            .border-blue-600 {
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }

            .imagem-cidade-forca {
                background-image: url("<%= imagens.cidade %>") !important;
                background-size: 100% 100% !important;
                background-position: center !important;
                background-repeat: no-repeat !important;
                width: 100% !important;
                height: 300px !important;
                display: block !important;
                overflow: visible !important;
            }
        </style>
    </head>
    <body class="bg-white text-gray-900">
        <body class="bg-white text-gray-900">
            <!-- PÁGINA 1 - CAPA -->
            <%- include('components/capa') %>

            <!-- PÁGINA 2 - SUMÁRIO -->
            <%- include('components/sumario') %>

            <!-- PÁGINA 3 - OBJETIVO -->
            <%- include('components/objetivo') %>

            <!-- PÁGINA 4 - METODOLOGIA -->
            <%- include('components/metodologia') %>

            <!-- PÁGINA 5 - PANORAMA SITUACIONAL -->
            <%- include('components/panorama') %>
        </body>
    </body>
</html>
